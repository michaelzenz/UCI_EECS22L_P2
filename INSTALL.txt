#To install this program in commandline:
Compile code by running:
    mkdir build
    cd build
    cmake ..
    make
Run the program running
    cd ..
    ./bin/kchat-server


#To install this program using easy script:
To compile run this command:
    bash SuperSimpleInstall.sh
To run:
    ./bin/kchat-server port
